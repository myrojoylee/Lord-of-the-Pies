  <h1>This is a test for {{name}} and the information should be here {{detail}}</h1>
  <!--This code block will work when recipe data is defined? -->
            {{#if recipe.length}}
            <section class="Usrres card">
            <h3>Your recipies here:</h3>
            {{#each recipe}}
            <h4 class="rec-name">{{name}}</h4>
            <p>{{directions}}</p>
            {{/each}}
    </section>
    {{else}}
    <p class="norec">No recipes saved! Check out the site to find what you love!</p>
    {{/if}}
<!--Test page for user profile-->